{
  "name": "writers-nook-marketing",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Writers Nook marketing site + Cloudflare Pages Functions backend (self-contained; decoupled from the Tauri app).",
  "scripts": {
    "dev": "wrangler pages dev",
    "deploy": "wrangler pages deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "types": "wrangler types"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.58"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "typescript": "~5.8.3",
    "vitest": "^4.1.8",
    "wrangler": "^3"
  }
}
