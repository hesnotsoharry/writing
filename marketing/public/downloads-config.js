// ============================================================================
// Download URL configuration — PUBLIC installer artifact URLs.
// Loaded via a plain <script> tag BEFORE account.js so window.WN_DL is
// available when account.js wires the download buttons.
//
// Replace the placeholder URLs with the real hosted installer paths at
// deploy time when build artifacts are published.
// ============================================================================
window.WN_DL = {
  macUrl: "https://downloads.writersnook.app/WritersNook-1.0.0.dmg",
  winUrl: "https://downloads.writersnook.app/WritersNook-1.0.0-setup.exe",
};
