# Bug audit 2026-08-27 — fix campaign tracker

Source: ultracode audit (69 agents, adversarially verified). Full report: https://claude.ai/code/artifact/d3e81424-2772-4ae0-a87b-b1662076beac
Rules: read the artifact entry before fixing (failure scenario + verifier trace). Line numbers may have drifted. `[U]` = unverified lead — confirm the bug exists before fixing. Do NOT touch src/features/ai/** or mobile/app.json (concurrent work). One commit per package or per coherent fix. Gates per charter: eslint + tsc + touched tests.

## P0 — Mobile catch-up corruption (CRITICAL)  _(owner: Claude (subtle CRDT/port lifecycle))_

- [x] **P0.1** _(live verification on the real pair still needed - Cole sync session)_ (critical) `mobile/src/features/editor/SceneEditorHost.tsx:100` — Catch-up with the scene open leaves a stale WebView doc that corrupts the epoch replacement

## P1 — Desktop sync engine + epochs  _(owner: Claude (subtle; worker assist where mechanical))_

- [x] **P1.1** (high) `src/App.tsx:72` — bindPersistence unbind-flush overwrites a just-applied epoch replacement, resurrecting discarded prose fleet-wide
- [ ] **P1.2** (high) `src/sync/epochManager.ts:48` — Diff frames carry only epoch n, not owner d — concurrent restores still merge instead of one winning
- [x] **P1.3** (high) `src/App.snapshots.ts:172` — Snapshot restore raced by bindPersistence unbind-flush: a second restore (or restore soon after typing) silently no-ops
- [ ] **P1.4** (medium) `src/sync/engine.ts:314` — Targeted single-doc hello makes every peer broadcast its entire library as full states
- [x] **P1.5** (medium) `src/sync/epochFrames.ts:52` — Outbox doc entries for any epoch>0 scene can never be acknowledged
- [x] **P1.6** (medium) `src/sync/frameCodec.ts:126` — Chunk-group TTL measured from creation, not last activity — >30s transfers can never reassemble
- [x] **P1.7** (medium) `src/sync/engine.ts:110` — start() has no in-flight guard and stop() cannot cancel a start in progress
- [x] **P1.8** (medium) `src/sync/epochManager.ts:79` — A restoring device never infers ownership from known.d == own deviceId — a missed recordLocal deadlocks the scene fleet-wide
- [x] **P1.9** (medium) `src/sync/provider.ts:92` — No dead-socket detection: Android background/network change wedges sync while status shows connected
- [x] **P1.10** (medium) `src/App.snapshots.ts:198` — Undo Replace-All restores the newest auto-snapshot, which a scene-leave auto-snap can shadow — undo silently no-ops
- [x] **P1.11** [U] (low) `src/sync/provider.ts:104` — Backoff resets on socket open — accept-then-close produces a 500ms reconnect storm

## P2 — LWW row sync  _(owner: Grok, Claude review)_

- [x] **P2.1** (high) `src/sync/lww/publisher.ts:12` — HLC clock resets on app restart; local edits silently refused after peer clock skew, then lost without preservation
- [x] **P2.2** (high) `src/sync/lww/reconciler.ts:200` — receiveRow stamps the ledger before projection; any projection failure permanently drops the row's content with no retry
- [x] **P2.3** (medium) `src/sync/lww/reconciler.ts:68` — Per-scope summary state has no sender key: three-device rooms interleave paged summaries, causing missed pushes and repeated bulk re-sends
- [x] **P2.4** [U] (low) `src/sync/lwwDomains/index.ts:43` — Boards tombstone deletes only the boards row; the board's Yjs doc survives, keeps syncing, and is re-seeded back onto the deleting device

## P3 — Meta/bible doc sync  _(owner: Grok, Claude review)_

- [ ] **P3.1** (high) `src/sync/remoteUpdateRouter.ts:41` — Meta doc store lost-update race between local bridge writes and remote merge
- [ ] **P3.2** (high) `src/sync/meta/localBridge.ts:52` — Deterministic fractional sortKey collision makes keyBetween throw, silently dropping meta mutations
- [ ] **P3.3** (high) `src/sync/meta/applyPlan.ts:126` — Scene upsert keeps folderId of a tombstoned/absent folder, orphaning the scene invisibly
- [ ] **P3.4** (medium) `src/sync/meta/applyPlan.ts:60` — Sort rewrites mix canonical (index+1)*1000 values with stale gapped sort_orders, diverging binder order
- [ ] **P3.5** (medium) `src/sync/meta/applyExec.ts:25` — applyMetaDoc's stale-snapshot upserts race local SQL edits and can revert them before they reach the CRDT
- [ ] **P3.6** (medium) `src/sync/bible/bibleLocalBridge.ts:97` — Bible delta capture swallows a local edit reverted mid-mutation by a concurrent remote apply
- [ ] **P3.7** (medium) `src/sync/engineDocRepository.ts:43` — Bible store lost-update between mergeDomain and BibleLocalBridge.mutate causes temporary resurrection/reverts

## P4 — Desktop persistence & export data loss  _(owner: Grok, Claude review)_

- [x] **P4.1** (high) `src/App.autoSnap.ts:51` — App close never flushes the pending debounced scene save — last <=500ms of typing silently reverts on next launch
- [x] **P4.2** (medium) `src/yjs/serialize.ts:77` — extractPlainText glues nested block boundaries (list items, blockquote paragraphs) with no separator — wrong word counts and missed whole-word search
- [x] **P4.3** (medium) `src/features/export/exportCollect.ts:18` — Export reads scene content straight from SQLite with no flush of the active scene's pending debounced save — recent keystrokes can be silently omitted
- [x] **P4.4** [U] (low) `src/editor/SpellCheckPopover.tsx:139` — Accepting a spelling suggestion strips all marks from the word — including aiExclude, un-hiding it from AI

## P5 — SQLite stores & migrations  _(owner: Grok, Claude review)_

- [x] **P5.1** (medium) `src/db/sqliteArchiveHelpers.ts:255` — Archive deletes scene prose docs before scene rows; a crash leaves visibly emptied scenes whose archive entry can never be restored
- [x] **P5.2** (medium) `src/db/migrations3.ts:55` — Migration 022 backfills quick_notes.state as 'inbox' for already-filed notes, resurrecting them in the mobile inbox
- [x] **P5.3** (medium) `src/db/sqliteBinderStore.ts:208` — deleteScene leaves scene_docs, scene_snapshots, scene_labels and scene_links orphaned; orphaned prose keeps replicating via sync
- [x] **P5.4** (medium) `src/db/sqliteBinderStore.ts:144` — deleteFolder moves scenes to Short pieces without renormalizing sort_order — tied keys interleave the list and diverge from the synced meta order
- [x] **P5.5** [U] (low) `src/db/sqliteGoalsStore.ts:86` — One-goal-per-(project, goal_type) invariant has no schema constraint, so LWW sync (and a local TOCTOU) create duplicate goals
- [ ] **P5.6** _(NEEDS COLE: UNIQUE(project_id,goal_type) would break LWW pairing merge - needs a type-level merge rule first)_ [U] (low) `src/db/deviceId.ts:14` — getOrCreateDeviceId has no in-flight memoization; concurrent first calls hand out a device id that is never persisted
- [x] **P5.7** [U] (low) `src/db/sqliteBinderStore.ts:105` — createScene/createFolder derive sort_order from row COUNT — after any deletion the new item collides with or undershoots existing keys

## P6 — Binder & app-state UI  _(owner: Gemini, Claude review)_

- [x] **P6.1** (medium) `src/binder/BinderDrag.tsx:262` — Any parent re-render mid-drag clears liveItems, so the drop commits a no-op and the reorder is silently lost
- [x] **P6.2** (medium) `src/App.content.tsx:284` — Project switch leaves brainstorm view open on the previous project's board — cross-project content displayed and editable
- [x] **P6.3** (medium) `src/binder/buildTree.ts:38` — buildTree silently hides scenes whose folder_id references a missing folder — orphans produced mid-session vanish until app restart

## P7 — Mobile app & mobile sync  _(owner: split: Gemini (UI) / Claude (sync lifecycle))_

- [x] **P7.1** (medium) `mobile/src/features/corkboard/index.tsx:43` — Corkboard cards render from stale `ordered` state — status taps appear dead, content never refreshes
- [x] **P7.2** (medium) `mobile/src/features/pairing/PairScreen.tsx:104` — SecureStore/DB failure mid-pairing is swallowed and permanently wedges the QR scanner
- [x] **P7.3** (medium) `mobile/src/features/settings/SyncCard.tsx:86` — Unpair during in-flight engine start leaves the engine syncing with a forgotten key
- [ ] **P7.4** [U] (medium) `mobile/src/features/editor/bridgeRouting.ts:51` — Guest bridge errors hard-fail the editor regardless of sessionId or the recoverable flag
- [ ] **P7.5** [U] (medium) `mobile/src/db/mobileArchiveStore.ts:58` — Archiving/deleting the currently-open scene never closes the live editor port — late keystrokes are silently lost
- [ ] **P7.6** [U] (low) `mobile/src/db/mobileEpochOwner.ts:54` — Mobile restore enqueues the replacement scene body durably, bypassing the converged-owner rule
- [ ] **P7.7** [U] (low) `mobile/src/db/mobileEpochOwner.ts:39` — Archive restore partially applies then throws on a project with no meta doc — restored scene appears empty

## P8 — Rust shell  _(owner: Claude (security-adjacent), Grok for mechanical)_

- [x] **P8.1** (high) `src-tauri/src/byok_engine.rs:194` — run_stream follows HTTP redirects, bypassing the W45 loopback/https guardrail and forwarding x-api-key
- [x] **P8.2** (medium) `src-tauri/src/sync_key.rs:42` — Keyring errors are conflated with 'no key' — a paired device can silently overwrite its sync master key
- [ ] **P8.3** (medium) `src-tauri/src/lib.rs:41` — backup_database copies the live SQLite file without quiescing writes — torn/corrupt backup race
- [x] **P8.4** (medium) `src-tauri/src/byok_engine.rs:201` — Cancellation is not honored during the connect/header phase and the client has no timeouts — Stop cannot abort a stalled request
- [x] **P8.5** [U] (low) `src-tauri/src/lib.rs:160` — Second .setup() call silently replaces the first — apply_window_rounding never runs
- [ ] **P8.6** _(DEFERRED: enabling CSP needs a tauri-dev smoke to validate against TipTap/inline styles - not safe to flip blind; low severity)_ [U] (low) `src-tauri/tauri.conf.json:25` — CSP disabled (csp: null) while high-privilege IPC commands are exposed to the webview

## P9 — Billing, webhooks, marketing  _(owner: Claude only (money))_

- [x] **P9.1** (high) `marketing/functions/api/ai/chat.ts:375` — Double refund in runStream catch path mints credits (balance inflation)
- [x] **P9.2** (medium) `marketing/functions/api/webhooks/lemon-squeezy.ts:38` — order_created upsert nulls a stored license_key on out-of-order or retried delivery
- [x] **P9.3** (medium) `marketing/functions/api/webhooks/lemon-squeezy-subscription.ts:98` — Refunding a top-up order never claws back the 600,000 granted credits
- [x] **P9.4** (medium) `marketing/public/account.js:188` — Account purchases query has no ORDER BY or status filter on a table that holds multiple rows per email
- [x] **P9.5** (low) `marketing/public/purchase-success-render.js:38` — Unsigned URL-param fallback lets anyone render fabricated purchase data on the trusted domain
- [x] **P9.6** [U] (low) `marketing/functions/api/webhooks/lemon-squeezy-subscription.ts:257` — Subscription key email is unrecoverable if the Resend send fails once
- [x] **P9.7** [U] (low) `marketing/functions/api/contact.ts:34` — Contact form interpolates attacker HTML unescaped into the operator email

## P10 — License & trial gating  _(owner: Claude only)_

- [x] **P10.1** (medium) `src/features/license/license.gate.ts:48` — Stale pre-purchase trial record turns any transient DB error into a hard lockout for activated users
- [x] **P10.2** (low) `src/features/license/trialRecordStore.ts:44` — A malformed or missing trial row is treated as 'never had a trial', silently granting a fresh 14-day trial
- [x] **P10.3** _(accepted risk: crash window between LS activate and local save is ~ms; recoverable via LS dashboard deactivation - no code change)_ (low) `src/features/license/activate.ts:69` — Interrupted or retried activation attempts permanently burn Lemon Squeezy activation seats with no release path

## P11 — Relay worker  _(owner: Gemini, Claude review)_

- [x] **P11.1** (low) `relay-worker/src/RelayRoom.ts:20` — Capacity-rejected socket remains an accepted room member that can still inject broadcasts
- [x] **P11.2** _(verified already strict - no change needed)_ (low) `relay-worker/src/index.ts:19` — Unbounded Durable Object creation on a live public endpoint - no rate limit or spend bound
- [x] **P11.3** _(server half; client pings land with the provider liveness fix)_ [U] (low) `relay-worker/src/RelayRoom.ts:19` — No keepalive or dead-socket reaping: silently-dead connections count toward the 8-socket cap and can wedge a room with 1013s
